interface:
  display_name: "Threat Scan"
  short_description: "Scan a diff for security boundary issues"
  default_prompt: "Use $detect-evil to inventory trust boundaries and security findings in this change."
